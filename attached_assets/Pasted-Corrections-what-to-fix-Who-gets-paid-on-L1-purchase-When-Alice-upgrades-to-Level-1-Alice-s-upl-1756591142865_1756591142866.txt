Corrections (what to fix)

Who gets paid on L1 purchase:
When Alice upgrades to Level 1, Alice’s upline (depth 1 above Alice) gets $100 (not Alice).
Rewards are always per-root: when your Layer N member upgrades to Level N, you (the root) get that Level-N reward (if you hold ≥ that level).

Alice as root — Layer 1 (L, M, R):
When Bob/Middle (Layer1) buys Level 1 → Alice gets $100 (claimable).
When Charlie/Left (Layer1) buys Level 1 → Alice gets $100 (claimable).
When David/Right (Layer1) buys Level 1 → Alice gets $100 (PENDING) → unlocks after Alice upgrades to Level 2; pending window = 72h (admin-config).

Layer 2 rule:
When a member in Alice’s Layer 2 upgrades to Level 2, Alice gets $150, but only if Alice is Level 2.

Platform revenue:
Only Level-1 purchases add +$30 per purchase to platform revenue (Levels 2–19 = $0 unless overridden).

Corrected mini test (step-by-step)

Alice upgrades to Level 1

Alice’s upline gets $100.

Platform +$30 (L1 purchase event).

Bob (Alice’s Layer1–L) upgrades to Level 1

Alice gets $100 (claimable).

Platform +$30.

Charlie (Alice’s Layer1–M) upgrades to Level 1

Alice gets $100 (claimable).

Platform +$30.

David (Alice’s Layer1–R) upgrades to Level 1

Alice gets $100 (PENDING) → unlocks when Alice reaches Level 2 (72h countdown applies).

Platform +$30.

👉 At this point, Alice’s Layer-1 total = $300 (2 claimable + 1 pending).
Platform revenue across these 4 L1 purchases = $120 (4 × $30).

Next placements:
After Alice’s Layer 1 (L, M, R) is filled, new activated L1 members (from Alice’s direct or global spillover) fill Alice’s Layer 2 in order: Left → Middle → Right across the layer, then proceed to deeper layers—always fill the lowest layer first.

Layer 2 rewards:
When any Layer 2 member upgrades to Level 2, Alice earns $150 only if she has upgraded to Level 2; otherwise it’s pending (72h) and then reallocated upward if still unqualified.

Acceptance checks (quick)

L1 events pay the purchaser’s upline $100; platform +$30.

As root, you earn when your Layer N upgrades to Level N.

Layer-1 Right reward is pending until root is L2 (72h gate).

Eligibility: root must hold ≥ N to confirm Layer-N rewards; else pending → reallocate upward after 72h.

Placement: strict L → M → R, fill current layer fully before moving to next.